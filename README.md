## Setup

1. Run `./install_moniserver.sh`. This will create a directory called `moniserver`

## Deploy to WinterNode

1. log into https://gcp.winternode.com/server/<>
1. Go to SFTP Details and press "Launch SFTP"
1. Delete all files besides the following:
    1. (dir) backups
    1. (dir) TTPD (or your world name)
    1. mc-wrapper.jar
    1. forge-1.20.1-47.4.16-installer.jar
1. Upload all the other files to the 
